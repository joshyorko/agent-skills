interface:
  display_name: "Rails 37signals Review"
  short_description: "Review Rails code the 37signals way"
  default_prompt: "Use $rails-37signals-review to review this Rails change for 37signals-style conventions."

policy:
  allow_implicit_invocation: true
