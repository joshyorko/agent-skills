# RCC Plugin Robocorp Documentation Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refresh the RCC plugin documentation so it accurately guides agents through modern Robocorp Python libraries, RPA Framework/HITL usage, Action Server/Sema4AI packages, and work-item/runtime patterns.

**Architecture:** Keep canonical content in `plugins/rcc/skills/...` and rebuild generated views after implementation. Split edits by documentation lane so each task has a focused write set and validation command.

**Tech Stack:** Markdown skill references, YAML templates, Python helper script, repository generators, `bin/check`, `rg`.

---

## File Structure

- Modify `plugins/rcc/skills/rcc/references/python-library-audit.md`: current source snapshot, package versions, Robocorp Python examples, RPA Framework split, and Action Server/work-items evidence.
- Modify `plugins/rcc/skills/rcc/references/source-map.md`: May 23, 2026 source inventory, PyPI versions, Josh/Robocorp/Sema4AI commits, public runtime repos, and inference boundaries.
- Modify `plugins/rcc/skills/rcc/references/agent-prompt-examples.md`: routing prompts for RCC core, robots, work-items, and Action Server lanes.
- Modify `plugins/rcc/skills/rcc-core/references/rcc-source-recipes.md`: RCC gateway framing, holotree/prebuilt environment notes, and public on-demand runtime webhook contract.
- Modify `plugins/rcc/skills/rcc-robots/references/rcc-command-recipes.md`: worker/runtime env vars and command context for public runtime examples.
- Modify `plugins/rcc/skills/rcc-robots/references/robot-project-recipes.md`: current RPA Framework/Assistant guidance and modern-vs-legacy Python examples.
- Modify `plugins/rcc/skills/rcc-robots/assets/templates/package.yaml`: update `actions-work-items` example pin.
- Modify `plugins/rcc/skills/rcc-robots/assets/templates/hitl-assistant/conda.yaml`: update RPA Framework and Assistant pins.
- Modify `plugins/rcc/skills/rcc-robots/assets/templates/conda-browser.yaml`: mark the template Robot Framework browser-oriented or update stale `rpaframework` pin.
- Modify `plugins/rcc/skills/rcc-robots/scripts/env_check.py`: update Python version recommendation for current RPA Framework/Assistant guidance.
- Modify `plugins/rcc/skills/rcc-workitems/references/workitems-adapters.md`: separate `robocorp-adapters-custom` from `actions-work-items`, add adapter/env matrix, process/reporter patterns, and retry guidance.
- Modify `plugins/rcc/skills/action-server/references/action-server-recipes.md`: update Action Server work-items package version, aliases, datadir behavior, and selection guidance.
- Generated by command: `skills/`, `.agents/skills/`, `.agents/plugins/marketplace.json`, `.claude-plugin/marketplace.json`, and `plugins/*/.claude-plugin/plugin.json` when the repository generators update them.

### Task 1: Refresh Source Map And Python Library Audit

**Files:**
- Modify: `plugins/rcc/skills/rcc/references/source-map.md`
- Modify: `plugins/rcc/skills/rcc/references/python-library-audit.md`

- [x] **Step 1: Capture current stale evidence markers**

Run:

```bash
rg -n '2026-05-01|7e63577|2e0e309|actions-work-items.: `0.2.1`|robocorp-adapters-custom.: `0.1.5`|rpaframework.: `31.2.0`|rpaframework-assistant.: `5.0.0`' plugins/rcc/skills/rcc/references/source-map.md plugins/rcc/skills/rcc/references/python-library-audit.md
```

Expected: matches for stale May 1 source snapshot, Josh commit references, and old PyPI package versions.

- [x] **Step 2: Update `source-map.md` May 23 snapshot**

Edit `plugins/rcc/skills/rcc/references/source-map.md` so the top-level current section records:

```markdown
## 2026-05-23 Robocorp / Sema4AI Documentation Refresh

This refresh reviewed modern Robocorp Python libraries, RPA Framework, Action Server/Sema4AI packages, Josh's RCC and actions forks, work-item adapters, and public Robocorp runtime/process examples. Use source-backed claims for package/API guidance. Mark Control Room backend architecture as inference when it is derived from public clients/examples.
```

Include source bullets for these reviewed heads:

```markdown
- `https://github.com/robocorp/robocorp` at `64cc9a3ecfd4`: modern `robocorp.*` packages.
- `https://github.com/robocorp/rpaframework` at `cc26f1a94505`: RPA Framework 32.0.0 release batch and `RPA.*` libraries.
- `https://github.com/Sema4AI/actions` at `9d9479e842ef`: Action Server 3.2.0, `sema4ai-actions`, and `sema4ai-mcp`.
- `https://github.com/joshyorko/actions` branch `community` at `3bae23bb49fe`: `actions-work-items` 0.2.4 and workflow-producer-consumer template.
- `https://github.com/joshyorko/rcc` at `59011b848ee1`: RCC docs, holotree, freeze/export, endpoint, and gateway behavior.
- `https://github.com/joshyorko/robocorp_adapters_custom` at `c56c70102423`: production-style SQLite, Redis, DocumentDB/MongoDB, and Yorko Control Room adapters.
```

- [x] **Step 3: Add public Robocorp org runtime/process sources to `source-map.md`**

Add a subsection named `Public Robocorp Runtime And Process Examples` with these bullets:

```markdown
- `robocorp/k8s-on-demand-runtimes` at `3969fc640768034011a712ae69a9d2bb082e0c71`: public Control Room on-demand runtime webhook shape, HMAC validation, Kubernetes Job provisioning, one-time runtime link token, and worker env vars.
- `robocorp/azure-on-demand-runtimes` at `5ead74cf2851ca773348d56897709e58349294e3`: Azure Functions/Durable Functions lifecycle orchestration for ephemeral runtimes and the same `start|stop|status` request family.
- `robocorp/example-worker-logs` at `8574600a9c5e721e4c531367b98894a877a93b56`: Workforce Agent log collection and Control Room artifact upload context.
- `robocorp/template-python-workitems` at `b7f25a819e8ccd3e620c741198e43c8145006d47`: modern Python producer/consumer work-item shape.
- `robocorp/python-producer-consumer-reporting` at `f7bf539`: producer/consumer/reporter flow, sentinel reporter item, and Process API reporting.
```

Add this warning immediately after:

```markdown
Public Control Room-adjacent examples support client-contract inference, not unreleased backend implementation claims. Keep HMAC, runtime token, worker env var, process-step, and queue semantics labeled as public contract or inference from examples.
```

- [x] **Step 4: Update `source-map.md` PyPI metadata block**

Replace stale package version bullets with:

```markdown
- `rpaframework`: `32.0.0`
- `rpaframework-assistant`: `6.0.0`
- `robocorp`: `3.1.1`
- `robocorp-browser`: `2.4.0`
- `robocorp-workitems`: `1.5.0`
- `robocorp-tasks`: `4.1.1`
- `robocorp-log`: `3.1.2`
- `robocorp-vault`: `1.4.0`
- `robocorp-storage`: `1.1.0`
- `sema4ai-action-server`: `3.2.0`
- `sema4ai-actions`: `1.6.6`
- `sema4ai-mcp`: `0.0.3`
- `actions-work-items`: `0.2.4`
- `robocorp-adapters-custom`: `0.1.6`
```

- [x] **Step 5: Update `python-library-audit.md` date and library map**

Change the opening paragraph to say it captures the `2026-05-23` audit. Update the library map rows so:

```markdown
| Work item queues | `robocorp-workitems`, `actions-work-items`, `robocorp-adapters-custom` | Producer/consumer/reporter flows, Action Server/non-robot queues, file/SQLite/Redis/DocumentDB adapters | Use `robocorp.workitems` for modern RCC robot flows, `actions-work-items` for Action Server/non-robot flows, and `robocorp-adapters-custom` for production-proven custom adapters such as DocDB-backed robot queues. |
| RPA Framework and HITL | `rpaframework`, `rpaframework-assistant`, Robot Framework libraries | Legacy `RPA.*` robots, Python-callable RPA utilities, Assistant desktop dialogs | Prefer modern `robocorp.*` libraries for new RCC Python robots. Use `RPA.Assistant` through `rpaframework-assistant` when a desktop/HITL dialog is intentionally part of the workflow. |
```

- [x] **Step 6: Add modern Robocorp example snippets to `python-library-audit.md`**

Ensure the file contains short examples for all of these APIs:

```python
from pathlib import Path
from robocorp.tasks import get_output_dir, setup, task

@setup(scope="session")
def prepare(_tasks):
    Path(get_output_dir()).mkdir(parents=True, exist_ok=True)

@task
def main():
    Path(get_output_dir(), "result.txt").write_text("ok", encoding="utf-8")
```

```python
from robocorp import log, vault

with log.suppress_variables():
    secret = vault.get_secret("service-account")
```

```python
from robocorp import storage

state = storage.get_json("workflow-state")
state["status"] = "done"
storage.set_json("workflow-state", state)
```

```python
from robocorp import log

log.process_snapshot()
```

For work-item attachments, include:

```python
from robocorp import workitems

item = workitems.outputs.create({"status": "ready"}, save=False)
item.add_file("output/report.json", name="report.json")
item.save()
```

- [x] **Step 7: Verify Task 1 edits**

Run:

```bash
rg -n '2026-05-23|rpaframework.: `32.0.0`|rpaframework-assistant.: `6.0.0`|actions-work-items.: `0.2.4`|robocorp-adapters-custom.: `0.1.6`|process_snapshot|outputs.create\(\{"status": "ready"\}, save=False\)|k8s-on-demand-runtimes|azure-on-demand-runtimes' plugins/rcc/skills/rcc/references/source-map.md plugins/rcc/skills/rcc/references/python-library-audit.md
```

Expected: matches for every current version, modern snippet, and runtime source.

### Task 2: Refresh RPA Framework Robot Docs, Templates, And Version Checks

**Files:**
- Modify: `plugins/rcc/skills/rcc-robots/references/robot-project-recipes.md`
- Modify: `plugins/rcc/skills/rcc-robots/assets/templates/hitl-assistant/conda.yaml`
- Modify: `plugins/rcc/skills/rcc-robots/assets/templates/conda-browser.yaml`
- Modify: `plugins/rcc/skills/rcc-robots/scripts/env_check.py`

- [x] **Step 1: Locate stale RPA Framework references**

Run:

```bash
rg -n "rpaframework==31|rpaframework-assistant==5|Python 3.8|RPA_SECRET|RPA_WORKITEMS|RC_VAULT|rpaframework.org" plugins/rcc/skills/rcc-robots
```

Expected: current stale pins in robot recipes/templates and Python 3.8 guidance in `env_check.py`.

- [x] **Step 2: Update Assistant recipe in `robot-project-recipes.md`**

Change the Assistant/HITL package example to:

```yaml
dependencies:
  conda-forge:
    - python=3.12.11
    - uv=0.8.17
  pypi:
    - robocorp==3.1.1
    - robocorp-browser==2.4.0
    - rpaframework==32.0.0
    - rpaframework-assistant==6.0.0
```

Add this paragraph under the example:

```markdown
`RPA.Assistant` is still useful for desktop/HITL Python workflows, but it comes from the separate `rpaframework-assistant` package. New headless RCC Python robots should prefer `robocorp.tasks`, `robocorp.browser`, `robocorp.workitems`, `robocorp.vault`, and `robocorp.storage` unless the workflow intentionally needs an Assistant dialog.
```

- [x] **Step 3: Add modern-vs-legacy env var note to `robot-project-recipes.md`**

Add a short section named `Modern Robocorp Python Vs RPA Framework Libraries` with:

```markdown
Modern Python robots use `RC_WORKITEM_*` and `RC_VAULT_*` variables with `robocorp.workitems` and `robocorp.vault`. Legacy Robot Framework/RPA Framework flows use `RPA_WORKITEMS_*` and `RPA_SECRET_*` variables with `RPA.Robocorp.WorkItems` and `RPA.Robocorp.Vault`. Do not mix the two env families in one run unless bridging old code intentionally.
```

- [x] **Step 4: Update HITL Assistant template pins**

Edit `plugins/rcc/skills/rcc-robots/assets/templates/hitl-assistant/conda.yaml` to use:

```yaml
      - rpaframework==32.0.0
      - rpaframework-assistant==6.0.0
      - robocorp-adapters-custom==0.1.6
```

- [x] **Step 5: Fix browser template stale pin or label**

In `plugins/rcc/skills/rcc-robots/assets/templates/conda-browser.yaml`, replace `rpaframework==31.2.0` with `rpaframework==32.0.0` and add a nearby comment:

```yaml
      # Robot Framework browser template. For modern Python browser tasks prefer robocorp-browser.
```

- [x] **Step 6: Update Python version check**

Edit `plugins/rcc/skills/rcc-robots/scripts/env_check.py` so the message currently returning `Python 3.8+ recommended` becomes:

```python
return False, f"Python 3.10+ recommended for current Robocorp/RPA Framework packages: {version_str}"
```

Keep the surrounding control flow unchanged unless the existing version tuple check must be aligned to `>= (3, 10)`.

- [x] **Step 7: Verify Task 2 edits**

Run:

```bash
rg -n "rpaframework==32.0.0|rpaframework-assistant==6.0.0|robocorp-adapters-custom==0.1.6|Python 3.10-3.13 required|Modern Robocorp Python Vs RPA Framework Libraries|Robot Framework browser template" plugins/rcc/skills/rcc-robots
```

Expected: matches in robot recipe docs, templates, and `env_check.py`.

### Task 3: Refresh Work-Items And Action Server Guidance

**Files:**
- Modify: `plugins/rcc/skills/rcc-workitems/references/workitems-adapters.md`
- Modify: `plugins/rcc/skills/action-server/references/action-server-recipes.md`
- Modify: `plugins/rcc/skills/rcc-robots/assets/templates/package.yaml`

- [x] **Step 1: Locate stale work-item references**

Run:

```bash
rg -n "actions-work-items=0.2.1|actions-work-items|actions_work_items|RC_REDIS_URL|RC_WORKITEM_AUTO_APPEND_OUTPUT_SUFFIX|COMPLETED_CONSUMER_WORKITEMS_JSON|_TERMINAL_|robocorp-adapters-custom" plugins/rcc/skills/rcc-workitems/references/workitems-adapters.md plugins/rcc/skills/action-server/references/action-server-recipes.md plugins/rcc/skills/rcc-robots/assets/templates/package.yaml
```

Expected: stale `0.2.1` references and partial adapter coverage.

- [x] **Step 2: Add lineage section to `workitems-adapters.md`**

Near the top of `workitems-adapters.md`, add:

```markdown
## Adapter Lineage

`robocorp-adapters-custom` predates the generalized `actions-work-items` package. Treat it as the production-proven adapter layer for classic `robocorp.workitems` robot workflows, especially DocumentDB/MongoDB queue families, GridFS attachments, explicit output queues, orphan recovery, retry handling, and RCC producer/consumer/reporter runs.

`actions-work-items` is the later generalized package for Action Server and non-robot workflows. It offers Robocorp-style aliases and a similar API shape, but it has its own adapter contract and should not replace production `robocorp-adapters-custom` guidance.
```

- [x] **Step 3: Update `actions-work-items` section**

Change its package guidance to version `0.2.4` and include:

```python
from actions import workitems
from actions_work_items import workitems as alias_workitems
from actions.work_items import inputs, outputs
```

Add env var bullets for:

```text
RC_WORKITEM_ADAPTER
RC_WORKITEM_QUEUE_NAME
RC_WORKITEM_OUTPUT_QUEUE_NAME
RC_WORKITEM_FILES_DIR
RC_WORKITEM_DB_PATH
RC_WORKITEM_INPUT_PATH
RC_WORKITEM_OUTPUT_PATH
RC_REDIS_URL
DOCDB_URI
DOCDB_HOSTNAME
DOCDB_PORT
DOCDB_USERNAME
DOCDB_PASSWORD
DOCDB_DATABASE
RC_WORKITEM_AUTO_APPEND_OUTPUT_SUFFIX
RC_WORKITEM_FILE_SIZE_THRESHOLD
```

Add extras text:

```markdown
Install Redis or DocumentDB support with `actions-work-items[redis]`, `actions-work-items[docdb]`, `actions-work-items[documentdb]`, or `actions-work-items[all]`.
```

- [x] **Step 4: Add process/reporter/retry patterns to `workitems-adapters.md`**

Add a section named `Process And Reporter Patterns` with:

```markdown
- Producer tasks usually read one seed input and create child output items.
- Consumer tasks loop the previous step's outputs and release each item as completed or failed.
- Reporter tasks can consume consumer outputs, use a sentinel payload such as `{"TYPE": "Reporter"}`, or inspect prior step work items through a Process API pattern.
- Retry automation should target application, orchestrator, or unspecified/system failures. Do not retry business/data failures by default.
- Local file adapters simulate JSON queue handoff, but they do not reproduce all hosted scheduling semantics.
- Local reporter smoke tests can use an explicit `COMPLETED_CONSUMER_WORKITEMS_JSON` fixture when the project does not have a hosted Process API run to inspect.
- Email-triggered examples rely on parsed Control Room email payloads and files; raw email payload paths are historical/deprecated example material.
- Terminal marker payloads such as `_TERMINAL_=True` are process-control examples, not a general work-item requirement.
```

- [x] **Step 5: Update Action Server work-items recipe**

In `action-server-recipes.md`, replace `actions-work-items=0.2.1` with:

```yaml
    - actions-work-items>=0.2.4
```

Add text:

```markdown
Use `actions-work-items` for Action Server-centered producer/consumer flows and non-robot workflows. Use `robocorp.workitems` for RCC robot processes. Use `robocorp-adapters-custom` when the production robot workflow depends on custom backend adapters such as DocumentDB.
```

Add the Action Server datadir note:

```markdown
The community `workflow-producer-consumer` template stores its default SQLite queue under `ACTION_SERVER_DATADIR` or `SEMA4AI_ACTION_SERVER_DATADIR` by setting `RC_WORKITEM_DB_PATH`. Its `/api/work-items` UI/API path is SQLite/datadir-specific in the reviewed community branch; do not present it as a generic Redis/DocumentDB control plane.
```

- [x] **Step 6: Update template package comment**

In `plugins/rcc/skills/rcc-robots/assets/templates/package.yaml`, replace the commented example:

```yaml
    # - actions-work-items=0.2.1
```

with:

```yaml
    # - actions-work-items>=0.2.4
```

- [x] **Step 7: Verify Task 3 edits**

Run:

```bash
rg -n "Adapter Lineage|actions-work-items>=0.2.4|actions-work-items\\[redis\\]|actions_work_items|RC_REDIS_URL|RC_WORKITEM_AUTO_APPEND_OUTPUT_SUFFIX|Process And Reporter Patterns|COMPLETED_CONSUMER_WORKITEMS_JSON|_TERMINAL_|ACTION_SERVER_DATADIR|SQLite/datadir-specific" plugins/rcc/skills/rcc-workitems/references/workitems-adapters.md plugins/rcc/skills/action-server/references/action-server-recipes.md plugins/rcc/skills/rcc-robots/assets/templates/package.yaml
```

Expected: matches for updated package pin, aliases, env vars, lineage, process patterns, and Action Server datadir warning.

### Task 4: Refresh RCC Gateway, Runtime, And Prompt Routing Docs

**Files:**
- Modify: `plugins/rcc/skills/rcc-core/references/rcc-source-recipes.md`
- Modify: `plugins/rcc/skills/rcc-robots/references/rcc-command-recipes.md`
- Modify: `plugins/rcc/skills/rcc/references/agent-prompt-examples.md`

- [x] **Step 1: Inspect current gateway docs**

Run:

```bash
rg -n "holotree|freeze|rccremote|runtime|Control Room|RC_WORKER|runtimeLinkToken|rcc-core|rcc-workitems|action-server" plugins/rcc/skills/rcc-core/references/rcc-source-recipes.md plugins/rcc/skills/rcc-robots/references/rcc-command-recipes.md plugins/rcc/skills/rcc/references/agent-prompt-examples.md
```

Expected: existing RCC command/source references but missing public on-demand runtime contract details.

- [x] **Step 2: Add RCC gateway framing**

In `rcc-source-recipes.md`, add a section named `RCC As Gateway`:

```markdown
RCC is the environment and execution gateway: it resolves `robot.yaml`, `conda.yaml`, holotree environments, dependency freeze/export, templates, bundles, profiles/endpoints, and command execution. Control Room-adjacent runtime provisioning is a separate lifecycle layer that can ask a worker to link back to Control Room, but RCC remains the local environment boundary inside that worker.
```

- [x] **Step 3: Add public on-demand runtime contract**

In the same file, add:

```markdown
## Public On-Demand Runtime Contract

Public `robocorp/k8s-on-demand-runtimes` and `robocorp/azure-on-demand-runtimes` examples expose a provider-neutral webhook shape with `type` values `start`, `stop`, and `status`, plus `workspaceId`, `runtimeId`, `runtimeLinkToken`, and optional `maxLifetimeSeconds`. These examples support public contract guidance and architecture inference only; they are not a released Control Room backend implementation.

One-time `runtimeLinkToken` values affect retry semantics. Retrying infrastructure after a token is consumed may require a fresh Control Room request rather than rerunning the same worker container.
```

Add the caution:

```markdown
Do not copy public provisioner examples as production hardening guides. The reviewed Azure example logs request/secret material during validation, so production provisioners need stricter secret handling and logging policy.
```

- [x] **Step 4: Add runtime worker env vars**

In `rcc-command-recipes.md`, add:

````markdown
## Runtime Worker Environment Variables

Public on-demand runtime examples set worker/linking variables such as:

```text
RC_WORKER_NAME
RC_WORKER_LINK_TOKEN
ROBOCORP_HOME
RC_AGENT_TERMINATE_AFTER_RUN_MS
```

These variables belong to worker lifecycle/linking context. Do not confuse them with RCC dependency resolution inputs such as `robot.yaml`, `conda.yaml`, and holotree cache state.
````

- [x] **Step 5: Update prompt routing examples**

In `agent-prompt-examples.md`, add examples that route:

```markdown
- RCC holotree, freeze/export, endpoint, prebuilt environment, rccremote, or public runtime webhook questions to `$rcc-core`.
- `robot.yaml`, `conda.yaml`, `robocorp.tasks`, `robocorp.browser`, `robocorp.vault`, `robocorp.storage`, `RPA.Assistant`, or RPA Framework questions to `$rcc-robots`.
- Producer/consumer/reporter, `robocorp-adapters-custom`, `actions-work-items`, Redis, DocumentDB, or queue adapter questions to `$rcc-workitems`.
- `package.yaml` v2, `sema4ai-actions`, `sema4ai-mcp`, `/mcp`, or Action Server work-items template questions to `$action-server`.
```

- [x] **Step 6: Verify Task 4 edits**

Run:

```bash
rg -n "RCC As Gateway|Public On-Demand Runtime Contract|runtimeLinkToken|RC_WORKER_LINK_TOKEN|RC_AGENT_TERMINATE_AFTER_RUN_MS|production hardening|\\$rcc-core|\\$rcc-robots|\\$rcc-workitems|\\$action-server" plugins/rcc/skills/rcc-core/references/rcc-source-recipes.md plugins/rcc/skills/rcc-robots/references/rcc-command-recipes.md plugins/rcc/skills/rcc/references/agent-prompt-examples.md
```

Expected: all gateway, runtime, env var, and routing phrases are present.

### Task 5: Rebuild Views And Verify

**Files:**
- Generated: `skills/`
- Generated: `.agents/skills/`
- Generated: `.agents/plugins/marketplace.json`
- Generated: `.claude-plugin/marketplace.json`
- Generated: `plugins/*/.claude-plugin/plugin.json`

- [x] **Step 1: Run source-level stale search before generated rebuild**

Run:

```bash
rg -n "actions-work-items=0.2.1|rpaframework-assistant==5.0.0|rpaframework==31.2.0|Python 3.8\\+ recommended" plugins/rcc/skills
```

Expected: no matches in canonical RCC skill docs.

- [x] **Step 2: Rebuild marketplace metadata**

Run:

```bash
python3 scripts/build_marketplaces.py
```

Expected: command exits `0` and regenerates marketplace outputs.

- [x] **Step 3: Rebuild runtime views**

Run:

```bash
python3 scripts/build_runtime_views.py
```

Expected: command exits `0` and generated skill views remain symlinks.

- [x] **Step 4: Run repository validation**

Run:

```bash
bin/check
```

Expected:

```text
Ran 26 tests
OK
repo structure validated
```

- [x] **Step 5: Run final evidence search**

Run:

```bash
rg -n '2026-05-23|rpaframework==32.0.0|rpaframework-assistant==6.0.0|actions-work-items>=0.2.4|actions-work-items.: `0.2.4`|robocorp-adapters-custom.: `0.1.6`|Python 3.10-3.13 required|runtimeLinkToken|RC_WORKER_LINK_TOKEN|process_snapshot|Adapter Lineage|RCC As Gateway' plugins/rcc docs/superpowers
```

Expected: matches across the canonical RCC skill docs and plan/spec.

- [x] **Step 6: Inspect final git diff**

Run:

```bash
git diff --stat
git status --short
```

Expected: only planned canonical docs, planned template/script hygiene files, generated views, and Superpowers plan/spec files are changed.

- [x] **Step 7: Commit implementation**

Run:

```bash
git add plugins/rcc docs/superpowers skills .agents .claude-plugin
git commit -m "docs: refresh RCC Robocorp library guidance"
```

Expected: commit succeeds on branch `codex/rcc-plugin-doc-refresh`.
