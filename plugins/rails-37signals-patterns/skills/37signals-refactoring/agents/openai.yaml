interface:
  display_name: "37signals Refactoring"
  short_description: "Coordinate specialist Rails 37signals refactor skills when explicitly delegated"
  default_prompt: "Use $37signals-refactoring after a workflow skill chooses this pattern-level refactor path."

policy:
  allow_implicit_invocation: false
