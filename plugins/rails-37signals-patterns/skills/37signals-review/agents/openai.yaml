interface:
  display_name: "37signals Review"
  short_description: "Reviews code through a public 37signals-inspired Rails lens: rich models, CRUD controllers, focused concerns, and Hotwire fit."
  default_prompt: "Use $37signals-review when requesting code review, architecture audit, or quality analysis."

policy:
  allow_implicit_invocation: true
