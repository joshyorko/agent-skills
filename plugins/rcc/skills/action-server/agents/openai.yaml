interface:
  display_name: "Action Server"
  short_description: "Action Server packages, package.yaml v2, actions, MCP exposure, and secrets."
  default_prompt: "Use $action-server for ordinary Action Server packages, package.yaml v2, sema4ai-actions, sema4ai-mcp, OpenAPI/MCP validation, secrets, and dev tasks."
