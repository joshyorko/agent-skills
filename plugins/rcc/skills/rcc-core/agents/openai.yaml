interface:
  display_name: "RCC Core"
  short_description: "RCC CLI/source, holotree, cache, endpoints, templates, and diagnostics."
  default_prompt: "Use $rcc-core for RCC itself: command selection, source tree orientation, holotree/cache internals, endpoint/profile configuration, templates, bundles, remote cache/client behavior, and diagnostics before robot task code runs."
